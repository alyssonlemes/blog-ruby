<!DOCTYPE html>
<html>
<head>
  <title>Blog do Alysson Lemes</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  
  <!-- Adicione o Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <!-- Adicione os ícones do Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
</head>

<body class="d-flex flex-column" style="height: 100vh;">
  <!-- Header -->
  <div class="bg-primary text-white text-center position-relative mb-4 p-4">
    <!-- Texto centralizado -->
    <%= link_to root_path, class: 'text-white text-decoration-none' do %>
      <h1 class="display-4 m-0">Blog do Alysson Lemes</h1>
    <% end %>
    <p class="lead m-0">
      Um blog sobre programação, tecnologia e desenvolvimento web.
    </p>

    <!-- Login/Logout no canto superior direito -->
    <div class="position-absolute d-flex align-items-center" style="top: 10px; right: 20px;">
      <% if user_signed_in? %>
        <span class="text-white text-center">Olá, <%= current_user.email %></span>
        <%= link_to destroy_user_session_path, method: :delete, class: 'text-white ml-3' do %>
          <i class="bi bi-box-arrow-right" style="font-size: 1.5rem;"></i>
        <% end %>
      <% else %>
        <%= link_to new_user_session_path, class: 'text-white text-center' do %>
          <i class="bi bi-person-circle" style="font-size: 1.5rem;"></i>
          <div>Entrar</div>
        <% end %>
      <% end %>
    </div>
  </div>
  
  <!-- Conteúdo principal -->
  <div class="flex-grow-1 container">
    <% flash.each do |type, message| %>
      <div class="alert alert-<%= type == 'alert' ? 'danger' : 'success' %> alert-dismissible fade show" role="alert">
        <%= message %>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <% end %>

    <%= yield %>
  </div>

  <!-- Footer -->
  <footer class="bg-primary text-white text-center mt-5 p-2">
    Copyright @Alysson Lemes <%= Date.current.year %>
  </footer>

  <!-- Adicione o Bootstrap JS e dependências antes do fechamento da tag body -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
